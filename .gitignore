node_modules/
dist/
.turbo/
*.tsbuildinfo
backups/
.env
.env.local
*.pem

# Local media uploads (MEDIA_DIR default) — production points this at
# /var/lib/mindblown/media, outside the checkout.
.media/
