/**
 * @mindblown/server — Fastify API, WebSocket real-time sync, PostgreSQL persistence.
 *
 * Handles authentication, mutation application, broadcasting changes
 * to connected clients, and integration webhooks.
 */
export const PACKAGE_NAME = '@mindblown/server' as const;
