/**
 * @mindblown/views — Alternative views: Kanban, Gantt, List/Table, Calendar.
 *
 * All views render the same Node data from @mindblown/core.
 * Each view is a React component that reads from the shared store.
 */
export const PACKAGE_NAME = '@mindblown/views' as const;
